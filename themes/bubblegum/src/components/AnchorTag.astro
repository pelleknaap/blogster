---
import type { HTMLAttributes } from "astro/types"


interface Props extends HTMLAttributes<'a'> {
  href: string;
  children: any;
}

const { href, children } = Astro.props;
---

<a href={href} target="_about" rel="noopener">{children}</a>